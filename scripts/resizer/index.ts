import resizeDesktop from './resizeDesktop'
import resizeMobile from './resizeMobile'

const config = {
  resizeThumbnails: true,
  resizeWork: true,
  includePages: [],
  includeBreakpts: [],
}

resizeMobile(config)