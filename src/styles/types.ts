export type positionsType = {
  top?: string | number,
  bottom?: string | number,
  left?: string | number,
  right?: string | number
}

export type fontVarConfigType = {
  MONO?: number
  CASL?: number
  slnt?: number
  CRSV?: number
}

export type mediaPropsType = {
  $aspectRatio: string | number
  $hasLoaded: boolean
}